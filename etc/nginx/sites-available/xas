server {
    listen 0.0.0.0:8888;

    location / {
         include fastcgi_params;
         fastcgi_param PATH_INFO $fastcgi_script_name;
         fastcgi_param SCRIPT_NAME "";
         fastcgi_pass unix:/var/run/xas/xas.sock;
    }
}
